# Ubuntu SSH Enable Script

This script installs OpenSSH Server and enables SSH on Ubuntu.

## Usage

Clone the repository:

git clone https://github.com/YOUR_USERNAME/YOUR_REPO_NAME.git

Enter the folder:

cd YOUR_REPO_NAME

Make the script executable:

chmod +x enable-ssh.sh

Run the script:

./enable-ssh.sh
